// This file can be replaced during build by using the `fileReplacements` array.
// `ng build` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firestore: {
    apiKey: "AIzaSyCAriBacLLjy71QmkaOSdObUOxeN3WLYTE",
    authDomain: "control-fridens.firebaseapp.com",
    projectId: "control-fridens",
    storageBucket: "control-fridens.appspot.com",
    messagingSenderId: "920551376498",
    // appId: "1:920551376498:web:8613f89cb8c1c3ad4824eb"
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/plugins/zone-error';  // Included with Angular CLI.
